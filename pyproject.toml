[project]
name = "fantasy-nba-gm"
version = "0.1.0"
description = "AI Co-GM for H2H category fantasy basketball — historical data pipeline + skeleton decision engine + recommendation log."
requires-python = ">=3.11"
dependencies = [
    "nba_api>=1.4",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "ruff>=0.6",
]
# Read-only Yahoo league import. Optional so the core package, the replay harness, and the
# whole test suite stay installable and runnable without OAuth credentials.
yahoo = [
    "yahoo_fantasy_api>=2.9",
    "yahoo_oauth>=2.0",
]

[project.scripts]
fantasy-gm = "fantasy_gm.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["fantasy_gm*"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
